### ✅ تهيئة الهيكل التشريحي الكامل للنظام وفق البروتوكول الهندسي الصارم

وهذا يتضمن تنفيذ ما يلي **بالترتيب الدقيق**:

---

## 🧱 1. إنشاء البنية التحتية للمشروع (Monorepo Setup)
```bash
mkdir apex-platform && cd apex-platform
pnpm init -y
```

ثم إعداد البنية التالية بدقة مطلقة:
```
apex-platform/
├── apps/
│   ├── storefront/       # Next.js 14 (App Router)
│   ├── tenant-admin/     # React + Shadcn UI
│   ├── super-admin/      # React + Tremor
│   ├── marketing/        # Next.js Landing
│   └── mobile/           # React Native (Expo Bare)
├── packages/
│   ├── core/             # Shared types, constants
│   ├── data/             # Prisma schema, GraphQL resolvers
│   ├── security/         # Auth, Zod schemas, rate limiting
│   └── ui/               # Square UI + Shadcn wrappers
├── infra/
│   ├── docker/
│   └── k8s/
├── prisma/
│   └── schema.prisma     # ← هذا هو العمود الفقري
└── .github/workflows/
```

> ⚠️ **لا يُسمح بإنشاء أي صفحة أو ميزة قبل وجود هذا الهيكل.**

---

## 🔐 2. تطبيق "القواعد النظامية الدقيقة جدًا جدًا جدًا" من اليوم الأول

### أ. قاعدة العزل بين المستأجرين (Tenant Isolation)
- تعديل `prisma/schema.prisma` ليشمل في كل نموذج رئيسي:
  ```prisma
  model Product {
    id        String   @id @default(uuid())
    tenantId  String
    // ... fields
    @@unique([id, tenantId])
    @@index([tenantId])
  }
  ```

### ب. إعداد Zod Validation Layer
- إنشاء `packages/security/src/schemas/`  
  ويحتوي على:
  - `HomepageContentSchema.ts`
  - `ProductFilterSchema.ts`
  - `LoginSchema.ts`
  - ... إلخ لكل نقطة دخول

### ج. تفعيل TypeScript Strict Mode
- في `tsconfig.base.json`:
  ```json
  {
    "compilerOptions": {
      "strict": true,
      "noImplicitAny": true,
      "strictNullChecks": true,
      "exactOptionalPropertyTypes": true
    }
  }
  ```

---

## 🛡️ 3. تهيئة نظام الأمان الأساسي
- إعداد **Middleware عالمي** في `apex-engine` (NestJS backend) يتحقق من:
  ```ts
  // middleware/tenant-isolation.middleware.ts
  if (!req.headers['apex-tenant-id']) {
    throw new BadRequestException('Missing Apex-Tenant-ID');
  }
  ```
- ربط هذا الـ Middleware بكل resolver في GraphQL.

---

## 🧪 4. كتابة أول اختبار تكاملي (Integration Test)
قبل حتى إنشاء صفحة `/`:
```ts
// test/e2e/tenant-isolation.test.ts
it('rejects request without Apex-Tenant-ID', async () => {
  const res = await request(app).get('/api/products');
  expect(res.status).toBe(400);
});
```

---

## 📜 5. توثيق "عقد الالتزام الهندسي" داخل المشروع
إنشاء ملف `ENGINEERING_PROTOCOL.md` في الجذر يحتوي على:
- القواعد الذهبية (No `any`, DTOs, Tenant Isolation…)
- قائمة المخالفات التي تؤدي إلى رفض PR
- قالب PR Checklist الإلزامي

---

## 🌐 6. تهيئة البيئة الخارجية (Remote Deployment Setup)
يتم تنفيذ المشروع على سيرفر خارجي معزول لضمان جودة الأداء:
- **Server**: GCP Instance (`34.18.154.179`)
- **OS User**: `apex_admin` (معزول عن المشروع الحالي بسيرفر العميل)
- **Port Mapping**: استخدام نطاق `7000 - 7020` حصريًا لتجنب التضارب.

---

### 🎯 لماذا هذه الخطوة الأولى؟
لأن **APEX PLATFORM ليس مشروعًا تقليديًّا**، بل منصة SaaS متعددة المستأجرين تتطلب:
- **عزلًا مطلقًا** بين المتاجر (حتى لو كان هناك خطأ بشري).
- **قابلية التوسع** إلى مليون مستأجر.
- **أمانًا من الطبقة الأولى**، وليس كطبقة لاحقة.

> 💡 بدون هذه الأساسيات، أي كود تكتبه "يعمل على جهازي" فقط — وليس "يعمل في الإنتاج تحت ضغط عالمي".

---

### ✅ الخلاصة:  
**ابدأ بالهيكل + البروتوكول + القواعد — وليس بالواجهات أو الميزات.**  
الكود يأتي **بعد** أن يكون النظام مصممًا ليكون **آمنًا، معزولًا، وقابلًا للتوسع** من اليوم الأول.
