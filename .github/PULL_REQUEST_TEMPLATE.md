<!-- 
❗️ هذا قالب إلزامي. لا يُقبل PR بدون تعبئته كاملًا.
❗️ أي PR يفتقر إلى أحد الطبقات الثلاثة = رفض فوري.
-->

# 🧩 Atomic Feature Delivery Checklist

> كل ميزة يجب أن تشمل **الثلاث طبقات**. إذا لم تنطبق إحداها، اشرح السبب بوضوح.

## ✅ 1. Frontend (UI / UX)
- [ ] تم إنشاء/تعديل الملفات في `apps/`
- [ ] جميع المدخلات تم التحقق منها عبر Zod
- [ ] تم استخدام مكونات من `packages/ui` أو `shadcn/ui` فقط
- [ ] لا يوجد `any` في TypeScript
- [ ] تم كتابة Unit Tests (Vitest) + E2E Test (Cypress)

📁 **الملفات المتأثرة**:  
```
apps/...
```

## ✅ 2. Backend (API / Business Logic)
- [ ] تم إنشاء/تعديل Resolvers أو Services في `packages/` أو `services/`
- [ ] كل Endpoint يتحقق من `Apex-Tenant-ID`
- [ ] جميع المدخلات تم التحقق منها عبر Zod Schemas من `packages/security`
- [ ] تم كتابة Integration Tests (Supertest)

📁 **الملفات المتأثرة**:  
```
packages/...  
أو  
services/...
```

## ✅ 3. Database (Schema / Data Model)
- [ ] تم تحديث `prisma/schema.prisma` (إذا لزم)
- [ ] تم إنشاء Migration عبر `pnpm prisma migrate dev --name ...`
- [ ] كل نموذج يحتوي على `tenantId` و`@@unique([id, tenantId])`
- [ ] لا يوجد `ON DELETE CASCADE`

📁 **الملفات المتأثرة**:  
```
prisma/schema.prisma
prisma/migrations/...
```

## ✅ 4. الالتزام بالبروتوكول الهندسي
- [ ] قرأت ووافقت على [`ENGINEERING_PROTOCOL.md`](/ENGINEERING_PROTOCOL.md)
- [ ] لا توجد أسرار (Secrets) في الكود
- [ ] تم تشغيل: `pnpm lint && pnpm type-check && pnpm test`
- [ ] تم اختبار العزل بين المستأجرين يدويًّا (إذا كانت الميزة حساسة)

## 📝 ملاحظات إضافية
<!-- اشرح هنا أي استثناء (مثل: هذه الميزة لا تحتاج DB لأنها UI فقط) -->
